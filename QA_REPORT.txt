Social Studio v1.5 QA
=====================

Checks completed:
- app.js JavaScript syntax: PASS (node --check)
- duplicate HTML IDs: PASS (none)
- getElementById references: PASS (all present)
- version badge: PASS (v1.5)
- package structure: root frontend files only

Targeted review:
- video upload keeps sampled analysis frames and stores original Blob locally
- Firebase payload uses serializable video metadata only (Blob/object URL are not sent)
- short video-only Reels use original moving video
- mixed Reels can include original video segments + photo motion
- saved project restoration attempts IndexedDB video retrieval
- per-project delete removes only the selected project and only removes its stored video when no other saved project references it
- clear-all removes project metadata and stored video IDs
